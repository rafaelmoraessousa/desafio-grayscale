const hero = {
    id:1,
    name: 'Spider-Man',
    company:'Marvel'
};

showHero(
    hero.id,
    hero.name,
    hero.company
);

function showHero(id, name, company){
    console.log(showHero());
}